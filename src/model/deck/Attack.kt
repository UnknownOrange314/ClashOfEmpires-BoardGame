package model.deck

class Attack(aCards: ArrayList<Card>, dCards: ArrayList<Card>) {

    val attackerCards = aCards;
    val defenderCards = dCards;

    // TODO: Add implementation.
    fun attackSucceeds(): Boolean {
        return false;
    }
}